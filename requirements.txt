# Package versions used to produce the results of the article (Python 3.13).
# HiGHS 1.8.0 is bundled inside SciPy; no separate install is needed.
#
# These are the PyPI wheels, which link OpenBLAS: results agree with the
# published ones to about 1e-6. For bit-for-bit reproduction use the MKL stack
# in environment.yml.

numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
matplotlib==3.10.6
scikit-learn==1.7.2
clarabel==0.11.1
qpsolvers==4.9.0

# Optional, only for download_data.py
# yfinance==1.2.0
